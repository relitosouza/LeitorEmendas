# Supabase — get these from your project at https://supabase.com → Settings → API
SUPABASE_URL=https://your-project-id.supabase.co
# WARNING: service_role key bypasses Row Level Security — never expose publicly, server-side only
SUPABASE_KEY=your-service-role-key-here

# Public JSON API URL for Vereadores de SP amendments data
# Returns a JSON array (or object wrapping one) of amendment records
VEREADORES_API_URL=https://example.com/api/vereadores
